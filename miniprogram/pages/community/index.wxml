<view class="page">
  <view class="content">
    <!-- Articles -->
    <view
      wx:for="{{articles}}"
      wx:key="id"
      class="article"
    >
      <view class="article-title">{{item.title}}</view>
      <view class="article-list">
        <view
          wx:for="{{item.items}}"
          wx:key="id"
          class="article-card"
        >
          <view wx:if="{{item.status}}" class="article-badge {{'article-badge-' + item.statusType}}">
            <text class="article-badge-text">{{item.status}}</text>
          </view>
          <image class="article-image" src="{{item.image}}" mode="aspectFill" />
          <view class="article-content">
            <text class="article-title-text">{{item.title}}</text>
            <text class="article-description">{{item.description}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
