<view class="ui-drawer-mask {{show ? 'show' : ''}}" bindtap="onClose"></view>
<view class="ui-drawer-container {{show ? 'show' : ''}}">
  <view class="ui-drawer-header">
    <text class="ui-drawer-title">{{title}}</text>
    <view class="ui-drawer-close" bindtap="onClose">✕</view>
  </view>
  <view class="ui-drawer-content">
    <slot></slot>
  </view>
  <view class="ui-drawer-footer" wx:if="{{showFooter}}">
    <slot name="footer"></slot>
  </view>
</view>
