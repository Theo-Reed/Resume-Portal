<scroll-view 
  class="ui-scroll-view custom-class" 
  scroll-y="{{scrollY}}" 
  scroll-x="{{scrollX}}" 
  enhanced="{{true}}" 
  show-scrollbar="{{false}}"
  bindscroll="{{onScroll}}"
>
  <slot></slot>
</scroll-view>
